<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>MindfulConnect - Find a Therapist</title>
        <style>
            :root {
                /* Primary Colors */
                --primary-50: #f0f9ff;
                --primary-100: #e0f2fe;
                --primary-200: #bae6fd;
                --primary-300: #7dd3fc;
                --primary-400: #38bdf8;
                --primary-500: #0ea5e9;
                --primary-600: #0284c7;
                --primary-700: #0369a1;
                --primary-800: #075985;
                --primary-900: #0c4a6e;
                
                /* Neutral Colors (light theme default) */
                --neutral-50: #f8fafc;
                --neutral-100: #f1f5f9;
                --neutral-200: #e2e8f0;
                --neutral-300: #cbd5e1;
                --neutral-400: #94a3b8;
                --neutral-500: #64748b;
                --neutral-600: #475569;
                --neutral-700: #334155;
                --neutral-800: #1e293b;
                --neutral-900: #0f172a;
                
                /* Semantic Colors */
                --text-primary: var(--neutral-900);
                --text-secondary: var(--neutral-600);
                --bg-primary: var(--neutral-50);
                --bg-secondary: var(--neutral-100);
                --card-bg: white;
                --border-color: var(--neutral-200);
                
                /* Status Colors */
                --success: #10b981;
                --warning: #f59e0b;
                --danger: #ef4444;
                --info: #3b82f6;
                
                /* Theme toggle transition */
                --theme-transition: all 0.3s ease;
            }
            
            [data-theme="dark"] {
                /* Dark theme overrides */
                --text-primary: var(--neutral-100);
                --text-secondary: var(--neutral-300);
                --bg-primary: var(--neutral-900);
                --bg-secondary: var(--neutral-800);
                --card-bg: var(--neutral-800);
                --border-color: var(--neutral-700);
            }
            
            body {
                background-color: var(--bg-primary);
                color: var(--text-primary);
                transition: var(--theme-transition);
            }
            
            .text-gradient {
                background: linear-gradient(90deg, var(--primary-500), var(--primary-700));
                -webkit-background-clip: text;
                background-clip: text;
                color: transparent;
                display: inline;
            }
            
            /* Card Styles */
            .community-card {
                background-color: var(--card-bg);
                border: 1px solid var(--border-color);
                border-radius: 1rem;
                transition: var(--theme-transition);
                color: var(--text-primary);
            }
            
            .community-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 5px 15px rgba(0,0,0,0.1);
                border-color: var(--primary-300);
            }
            
            .community-card .card-body {
                padding: 2rem;
            }
            
            .community-card h3 {
                color: var(--primary-600);
                margin-bottom: 1rem;
            }
            
            /* Form Styles */
            .search-form .form-group {
                margin-bottom: 1rem;
            }
            
            .search-form .form-label {
                font-weight: 500;
                margin-bottom: 0.5rem;
                color: var(--text-primary);
            }
            
            .form-control, .form-select {
                background-color: var(--card-bg);
                border-color: var(--border-color);
                color: var(--text-primary);
                transition: var(--theme-transition);
            }
            
            .form-control:focus, .form-select:focus {
                border-color: var(--primary-500);
                box-shadow: 0 0 0 0.25rem rgba(14, 165, 233, 0.25);
            }
            
            /* Button Styles */
            .btn {
                transition: var(--theme-transition);
            }
            
            .btn-primary {
                background-color: var(--primary-600);
                border-color: var(--primary-600);
            }
            
            .btn-primary:hover {
                background-color: var(--primary-700);
                border-color: var(--primary-700);
            }
            
            .btn-outline-primary {
                color: var(--primary-600);
                border-color: var(--primary-600);
            }
            
            .btn-outline-primary:hover {
                background-color: var(--primary-600);
                color: white;
            }
            
            /* Badge Styles */
            .badge {
                padding: 0.5rem 1rem;
                font-weight: 500;
                font-size: 0.9rem;
                transition: var(--theme-transition);
            }
            
            .badge.bg-primary {
                background-color: var(--primary-600) !important;
                color: white;
            }
            
            .badge.bg-info {
                background-color: var(--info) !important;
                color: white;
            }
            
            .badge.bg-success {
                background-color: var(--success) !important;
                color: white;
            }
            
            /* Rating Styles */
            .rating {
                font-size: 1.1rem;
                font-weight: 500;
                color: var(--text-primary);
            }
            
            .rating .text-warning {
                color: var(--warning) !important;
            }
            
            /* Text Colors */
            .text-muted {
                color: var(--text-secondary) !important;
            }
            
            /* Layout Styles */
            .therapist-list {
                margin-top: 2rem;
            }
            /* Responsive Adjustments */
            @media (max-width: 768px) {
                .community-card .card-body {
                    padding: 1.5rem;
                }
                
                .d-flex.gap-2 {
                    gap: 0.5rem !important;
                }
            }
        </style>
    </head>
    <body>
        <%- include('partials/header-logged-in') %>

        <!-- Hero Section -->
        <section class="hero">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-10 text-center">
                        <h1 class="display-3">Find Your <span class="text-gradient">Therapist</span></h1>
                        <p class="subtitle mb-4">Connect with qualified mental health professionals who can support your journey</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Search Section -->
        <section class="features py-6">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-8">
                        <div class="community-card mb-4">
                            <div class="card-body">
                                <h3><i class="fas fa-search me-2"></i>Find a Therapist</h3>
                                <form action="/therapist/search" method="GET" class="search-form">
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <div class="form-group">
                                                <label class="form-label">Location</label>
                                                <input type="text" class="form-control" name="location" placeholder="City, State, or Online">
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-group">
                                                <label class="form-label">Specialization</label>
                                                <select class="form-select" name="specialization">
                                                    <option value="">All Specializations</option>
                                                    <option value="anxiety">Anxiety</option>
                                                    <option value="depression">Depression</option>
                                                    <option value="trauma">Trauma</option>
                                                    <option value="relationships">Relationships</option>
                                                    <option value="stress">Stress Management</option>
                                                    <option value="addiction">Addiction</option>
                                                </select>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-group">
                                                <label class="form-label">Therapy Type</label>
                                                <select class="form-select" name="therapyType">
                                                    <option value="">All Types</option>
                                                    <option value="individual">Individual Therapy</option>
                                                    <option value="couples">Couples Therapy</option>
                                                    <option value="group">Group Therapy</option>
                                                    <option value="online">Online Therapy</option>
                                                </select>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-group">
                                                <label class="form-label">Insurance</label>
                                                <select class="form-select" name="insurance">
                                                    <option value="">All Insurance Types</option>
                                                    <option value="private">Private Insurance</option>
                                                    <option value="medicare">Medicare</option>
                                                    <option value="medicaid">Medicaid</option>
                                                    <option value="sliding">Sliding Scale</option>
                                                </select>
                                            </div>
                                        </div>
                                        <div class="col-12">
                                            <button type="submit" class="btn btn-primary w-100">
                                                <i class="fas fa-search me-2"></i>Search Therapists
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>

                        <!-- Therapist Listings -->
                        <div class="therapist-list">
                            <% if (therapists && therapists.length > 0) { %>
                                <% therapists.forEach(therapist => { %>
                                    <div class="community-card mb-4">
                                        <div class="card-body">
                                            <div class="row">
                                                <div class="col-md-3">
                                                    <div class="text-center">
                                                        <div class="rating mb-2">
                                                            <i class="fas fa-star text-warning"></i>
                                                            <span class="ms-1"><%= therapist.rating %></span>
                                                            <small class="text-muted">(<%= therapist.reviews %> reviews)</small>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-md-9">
                                                    <h3 class="mb-2"><%= therapist.name %></h3>
                                                    <p class="text-muted mb-2">
                                                        <i class="fas fa-map-marker-alt me-2"></i><%= therapist.location %>
                                                    </p>
                                                    <div class="mb-3">
                                                        <div class="d-flex flex-wrap gap-2 mb-2">
                                                            <% therapist.specializations.forEach(spec => { %>
                                                                <span class="badge bg-primary"><%= spec %></span>
                                                            <% }); %>
                                                        </div>
                                                        <div class="d-flex flex-wrap gap-2 mb-2">
                                                            <% therapist.therapyTypes.forEach(type => { %>
                                                                <span class="badge bg-info"><%= type %></span>
                                                            <% }); %>
                                                        </div>
                                                        <div class="d-flex flex-wrap gap-2">
                                                            <% therapist.insurance.forEach(ins => { %>
                                                                <span class="badge bg-success"><%= ins %></span>
                                                            <% }); %>
                                                        </div>
                                                    </div>
                                                    <p class="mb-3"><%= therapist.bio %></p>
                                                    <div class="d-flex gap-2">
                                                        <a href="/therapist/<%= therapist._id %>" class="btn btn-primary">
                                                            <i class="fas fa-user me-2"></i>View Profile
                                                        </a>
                                                        <button class="btn btn-outline-primary" onclick="scheduleConsultation('<%= therapist._id %>')">
                                                            <i class="fas fa-calendar me-2"></i>Schedule Consultation
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="community-card">
                                    <div class="card-body text-center">
                                        <i class="fas fa-search fa-3x text-muted mb-3"></i>
                                        <h3>No Therapists Found</h3>
                                        <p>Try adjusting your search criteria to find the right therapist for you.</p>
                                    </div>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <%- include('partials/footer') %>
    </body>
</html>